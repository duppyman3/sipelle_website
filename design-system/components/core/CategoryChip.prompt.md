Round watercolor category chip with a hand-written label, used in a row of three on home.

```jsx
<CategoryChip image="assets/category-citrus.png" label="Citrus" onPress={...} />
```

Ship the artwork as pre-rendered PNGs (assets/category-*.png); the circle fill is part of the image.
